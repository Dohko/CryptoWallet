/**
 * @providesModule ui-styles-setup
 */

/** dynamic loading (require with dynamic string) doesn't work so we have to do like that... */

export const styles = {
	MainScreen: require('ui-style-mainscreen'),
};
